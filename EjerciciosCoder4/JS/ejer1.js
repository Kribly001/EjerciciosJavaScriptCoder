//Escribir una función que reciba un número y devuelva su cuadrado:

function calculocuadrado(numero) {
    return numero = Math.pow(numero, 2)
}

const numeroingresado = parseInt(prompt("Ingresa numero y se calculara el cuadrado"))
console.log(calculocuadrado(numeroingresado));